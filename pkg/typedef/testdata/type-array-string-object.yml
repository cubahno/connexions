openapi: 3.1.0
info:
  title: Type Array Test
  version: 1.0.0
paths:
  /test:
    get:
      operationId: getTest
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'

components:
  schemas:
    Response:
      type: object
      properties:
        id:
          type: string
        metadata:
          description: Arbitrary metadata
          type:
            - string
            - object
            - 'null'

